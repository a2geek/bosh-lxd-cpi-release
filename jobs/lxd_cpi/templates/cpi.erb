#!/bin/bash

pkgs_dir=${BOSH_PACKAGES_DIR:-/var/vcap/packages}
jobs_dir=${BOSH_JOBS_DIR:-/var/vcap/jobs}

cat cpi.json >&2

exec $pkgs_dir/lxd_cpi/bin/cpi -configPath $jobs_dir/lxd_cpi/config/cpi.json
